'use strict'

module.exports = {
    initialize : initialize
};

function initialize(app){
    //app.use('', auth.isAuthenticated, require('./ids-users/ids-users'));

    //var events = require('./events/events');
    //app.use('/', events);
}

